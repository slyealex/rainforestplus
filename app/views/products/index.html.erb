<h1>All Products</h1>
<div class="container-product">
  <% @products.each do |product| %>
    <p><%= product.name %></p>
    <p><%= product.description %></p><br/>
    <p>$<%= product.price_in_cents %></p>
    <p><%= link_to "View this product!", "/products/#{product.id}" %></p>
  <% end %>
</div>
